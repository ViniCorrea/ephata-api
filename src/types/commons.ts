export enum CivilStatus {
  Single,
  Married,
  Divorced,
  Widowed,
}
export enum Environment {
  Development = 'development',
  Testing = 'testing',
  Staging = 'staging',
  Production = 'production',
}
export enum Gender {
  Female,
  Male,
  Other,
  // Transgender,
  // TwoSpirit,
  // NonBinary,
  // Genderqueer,
  // GenderExpression,
  // GenderFluid,
  // GenderNeutral,
}
